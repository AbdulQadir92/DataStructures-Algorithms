

const arr = ['Abdul', 1, true, { a: 'a' }];

for (elem of arr) {
	console.log(elem);
}

// Big-O
// push / pop / access - O(1)
// shift / unshift / search / concat / slice / splice / forEach / map / filter / reduce - O(n)
